trigger: #none 
  batch: 'true'
  branches:
    include: [ dev ]
  tags:
    exclude: [ NoBuild ]

variables:
  ORG:        BRW
  PREFIX:     ACU1
  APP :       AOA
  ENV:        T5
  ADOProject: ADF

stages:
- stage: Deploy_All_${{ variables.PREFIX }}-${{ variables.ORG }}-${{ variables.APP }}-RG-${{ variables.ENV }}
  jobs:
  - template: ..\..\..\.ado\azure-pipelines-ALL.yml
    parameters:
      ORG:    ${{ variables.ORG }}
      PREFIX: ${{ variables.PREFIX }}
      APP :   ${{ variables.APP }}
      ENV:    ${{ variables.ENV }}
      SP:     ADO_${{ variables.ADOProject }}_${{ variables.PREFIX }}-${{ variables.ORG }}-${{ variables.APP }}-RG